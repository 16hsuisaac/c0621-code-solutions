/* exported getFirstChar */
function getFirstChar(string) {
  var firstChar = string[0];
  return firstChar;
}

/* First letter of a string will be the first character,
thus string at 0 will always be the first letter of single word
input */
